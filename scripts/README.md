# Scripts (dev utilities)

These are **development utilities**. They are not required for running the app.

- `seed_users.php` – creates demo users (admin + client) if they don't exist.
  - Run in browser: `http://localhost/web-agency-app/scripts/seed_users.php`
  - After seeding, log in with the credentials shown by the script.
